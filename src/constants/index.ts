export const APP_NAME = 'PrisonGuardApp';

export const API = {
  BASE_URL: 'https://api.example.com',
  TIMEOUT: 10000,
} as const;

export const STORAGE_KEYS = {
  AUTH_TOKEN: 'auth_token',
  USER_DATA: 'user_data',
} as const;
